#!/bin/bash

MYDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

source ${MYDIR}/../virtualenv/bin/activate

python ${MYDIR}/../src/ezcluster.py $*
err=$?

# Useless, as we will leave this shell
#deactivate

exit $err
