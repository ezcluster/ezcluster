
- hosts: all
  roles:
  - { role: disk_format, tags: [ disks ] }
  tasks:
  - name: Adjust logical volumes on root disk
    include_role:
      name: lvextend
    with_items: "{{ root_lvs }}"
    loop_control:
      loop_var: logical_volume
    tags: [ "disks" ]