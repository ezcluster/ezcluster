
{% for node in m.cluster.nodes -%}
{{ node.name }} ansible_ssh_host={{ node.ip }} ansible_ssh_user=root ansible_ssh_private_key_file={{ m.data.patternByName[node.pattern].kvmTemplate.build_key_pair }}		#  {{ node.fqdn }}
{% endfor %}
